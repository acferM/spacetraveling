import styled from "styled-components";

export const Container = styled.div`
  padding: 0 23.125rem;

  img {
    margin-top: 5rem;
  }

  main {
    margin-top: 4rem;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }
`