import styled from "styled-components";

export const Container = styled.article`
  h1 {
    color: #F8F8F8;
    font-size: 1.75rem;
  }

  p {
    font-size: 1.125rem;
    color: #D7D7D7;
    margin-top: 0.5rem;
  }

  footer {
    color: #BBBBBB;
    margin-top: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;

    div {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
  }
`